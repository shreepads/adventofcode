pub fn count_depth_inc(file_path: &String) -> i32 {
    println!("Loading data from file:{}", file_path);
    56
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);
    }
}
